# ts-rs's default export base. Nothing writes here any more — the destination
# lives in tests/generated_bindings.rs — but an accidental default-config export
# would land here, and it is not the artifact.
/bindings/
