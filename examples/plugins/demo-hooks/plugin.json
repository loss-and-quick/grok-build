{
  "name": "demo-hooks",
  "version": "0.1.0",
  "description": "Demo: a TypeScript sidecar plugin exercising the PreToolUse / Stop / SessionStart hooks plus one model-visible tool.",
  "exec": ["${GROK_PLUGIN_ROOT}/_sdk/run", "index.ts"],
  "network": false,
  "tools": [
    {
      "name": "echo",
      "description": "Echo the given text back, with the caller's context.",
      "inputSchema": {
        "type": "object",
        "properties": { "text": { "type": "string", "description": "Text to echo" } },
        "required": ["text"]
      }
    }
  ]
}
