{
  "name": "@grok-build/theme",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The pager's color palette and turn animation, generated from the Rust it renders from so a browser client cannot drift from the TUI.",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
